name: Create tags

on:
  push:
    branches: [ master ]
    tags: "!*"

jobs:
  create-tags:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Tag Package
        uses: Klemensas/action-autotag@1.2.3
        with:
          package_root: package/foo
          tag_prefix: "@foo/test/v"
